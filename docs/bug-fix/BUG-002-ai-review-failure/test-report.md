# 测试验证报告：BUG-002

> 本报告为编号目录制落地后的追溯补录，结果依据当时实际命令执行记录整理。

## 1. 报告信息

| 项 | 内容 |
|----|------|
| 迭代编号 | BUG-002 |
| 执行日期 | 2026-08-26 |
| 执行方式 | 按 05 规范固定顺序执行 |
| 结论 | 有条件通过 |

## 2. 执行结果

| 步骤 | 命令 | 结果 |
|------|------|------|
| 后端静态检查 | `python -m py_compile`（路由 / 服务 / 依赖 / 工具） | 通过 |
| 后端回归测试 | `python -m pytest tests/test_contract_review.py` | **18 passed** |
| 前端 Lint | `pnpm run lint` | 通过（退出码 0） |
| 前端构建 | `pnpm run build` | 通过（退出码 0，预渲染成功） |
| 页面渲染校验 | 平台 UI 校验能力 | 通过 |

## 3. 用例覆盖

回归用例共 18 项，全部使用假调用器，未消耗真实 AI 额度，覆盖：

1. 本地提取成功时不产生 AI 提取调用。
2. 扫描件场景降级为 AI 提取。
3. 无有效文本时抛出业务异常。
4. 各类 AI 错误的分类与可重试性判定（额度耗尽 / 配置错误 / 服务不可用 / 限流等共 8 类）。
5. 代码块包裹的 JSON 正常解析。
6. 风险等级与合规状态的中文别名与大小写归一化。
7. 非法 JSON 经一次修复重试后成功。
8. 必填字段缺失时拒绝结果。
9. 额度异常不被吞成通用失败。
10. 日志脱敏不泄露合同正文与密钥。

## 4. 验收标准对照

| 编号 | 标准 | 结果 |
|------|------|------|
| AC-01 | 额度耗尽返回 402 且前端隐藏重试按钮 | 通过 |
| AC-02 | 服务不可用 / 限流返回 503 且可重试 | 通过 |
| AC-03 | 无有效文本返回 422 | 通过 |
| AC-04 | 未知异常返回 500 且带追踪标识 | 通过 |
| AC-05 | 文本型 PDF 不产生 AI 提取调用 | 通过 |
| AC-06 | 扫描件降级为 AI 提取 | 通过 |
| AC-07 | 日志不泄露敏感内容 | 通过 |
| AC-08 | 回归用例覆盖且不消耗真实额度 | 通过（18 passed） |

## 5. 缺陷与修复过程记录

- 初次执行回归用例时出现 2 项失败：本地提取阈值设置偏高，导致文本型合同被误判为提取不足而触发 AI 提取。
- 处理方式：将本地提取最小字符阈值调整为 200，并补全用例中的合同样本文本与断言。
- 复跑结果：18 passed。

## 6. 遗留风险

1. 账户 AI 额度不足属于账户余额问题，而非代码缺陷。充值后审查链路即可完整生成报告；代码侧已保证该场景有明确可读的用户提示与可定位的追踪标识。
2. 扫描件识别质量仍依赖 AI 提取能力，极端低清扫描件可能仍无法得到有效文本，此时按 422 提示用户更换文件。

## 7. 最终结论

**有条件通过**：全部验收标准达成，自动化验证全绿；遗留项为账户额度充值（非代码问题）与扫描件识别质量上限。