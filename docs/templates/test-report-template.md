# 测试验证报告：<一句话标题>

> 复制本模板到 `docs/features/FEAT-<3位>-<slug>/test-report.md` 或 `docs/bug-fix/BUG-<3位>-<slug>/test-report.md`。
> **只允许填写真实执行结果。** 禁止把预期输出当作实际输出，禁止「预计通过」这类结论。
> 每个 FEAT / BUG 必须跑 Playwright，截图放到同级 `test-report/` 目录，文件名 `S01-<slug>.png` 起，下表逐张引用。

## 1. 报告信息

| 项 | 内容 |
|----|------|
| 迭代号 | `<YYYYMMDD>-<slug>` |
| 关联文档 | `./spec.md`、`./plan.md`、`./checklist.md` |
| 执行人 | |
| 执行日期 | |
| 被测版本 | <提交/构建标识> |
| 结论 | ✅ 通过 / ⚠️ 有条件通过 / ❌ 不通过 |

## 2. 环境信息

| 项 | 值 |
|----|-----|
| 前端 | React 18 + TypeScript + Vite + shadcn/ui |
| 后端 | FastAPI + SQLAlchemy Async + PostgreSQL |
| 运行环境 | Atoms 平台预览 / 本地 |
| Node / pnpm | |
| Python | |

## 3. 自动化执行结果

| 顺序 | 步骤 | 命令 | 退出码 | 结果 | 耗时 |
|------|------|------|--------|------|------|
| ① | 后端静态检查 | `python -m py_compile <files>` | | ✅ / ❌ | |
| ② | 后端测试 | `python -m pytest tests -q` | | ✅ / ❌ | |
| ③ | 前端 Lint | `pnpm i && pnpm run lint` | | ✅ / ❌ | |
| ④ | 前端单元测试 | `pnpm run test -- --run` | | ✅ / ❌ / 跳过（无用例） | |
| ⑤ | 前端构建 | `pnpm run build` | | ✅ / ❌ | |
| ⑥ | Playwright 端到端 | `pnpm run e2e` | | ✅ / ❌ | |

### 编号截图

截图目录：`./test-report/`（与本文件同级）。每张必须在此表引用。

| 编号 | 文件 | 覆盖 AC | 说明 |
|------|------|---------|------|
| S01 | `test-report/S01-<slug>.png` | | |

### 关键输出摘要

```text
<每步末尾关键输出，各步 ≤ 20 行。禁止粘贴完整日志。>
```

## 4. 测试用例统计

| 层级 | 总数 | 通过 | 失败 | 跳过 | 覆盖率 |
|------|------|------|------|------|--------|
| 单元测试 | | | | | |
| 集成测试 | | | | | |
| 合计 | | | | | |

### 新增用例清单

| 用例名 | 层级 | 覆盖点 | 结果 |
|--------|------|--------|------|
| | 单元 / 集成 | | ✅ / ❌ |

### 回归用例（Bug 迭代必填）

| 用例名 | 对应缺陷 | 结果 |
|--------|----------|------|
| | | ✅ / ❌ |

## 5. 验收标准逐条对照

| 编号 | 验收标准 | 验证方式 | 实际结果 | 结论 |
|------|----------|----------|----------|------|
| AC-01 | | 自动化 / 手工 | | ✅ / ❌ |
| AC-02 | | | | |

## 6. 异常场景验证

| 编号 | 场景 | 期望 | 实际 | 结论 |
|------|------|------|------|------|
| E-01 | 输入不合法 | 400 + 明确提示，不触发下游调用 | | ✅ / ❌ |
| E-02 | 未登录 | 引导登录 | | |
| E-03 | 依赖故障 | 500 + 通用提示 + 错误编号 | | |
| E-04 | 重复提交 | 幂等，无重复数据 | | |
| E-05 | 空数据 | 空态友好 | | |

## 7. 关键旅程走查

| 旅程 | 步骤 | 结果 | 备注 |
|------|------|------|------|
| 主链路 | 首页 → 登录 → 上传审查 → 报告详情 | ✅ / ❌ | |
| 历史管理 | 历史列表 → 查看报告 → 删除 | ✅ / ❌ | |
| 认证 | 登录回调 → 登出 → 登出回跳首页 | ✅ / ❌ | |

## 8. 缺陷清单

| 编号 | 描述 | 严重级别 | 复现步骤 | trace_id | 状态 |
|------|------|----------|----------|----------|------|
| D-01 | | 阻塞 / 严重 / 一般 / 轻微 | | | 已修复 / 遗留 |

## 9. 规范符合性核对

- [ ] 分层与复杂度阈值符合 [01](../../rules/01-development-standards.md)
- [ ] 日志与 `trace_id` 埋点符合 [02](../../rules/02-logging-and-tracing.md)，无敏感数据入日志
- [ ] 配置项已文档化且缺失可降级，符合 [03](../../rules/03-configuration.md)
- [ ] 事务不跨 AI 调用，符合 [06](../../rules/06-backend-layering.md) / [07](../../rules/07-database-acid.md)
- [ ] `checklist.md` 全部条目已勾选或已记录豁免

## 10. 遗留风险与建议

| 编号 | 遗留项 | 影响 | 建议处理时机 |
|------|--------|------|--------------|
| R-01 | | | 本迭代 / 下迭代 |

## 11. 结论

<明确写出：是否通过；若为「有条件通过」，列出条件与遗留项编号；若不通过，列出阻塞缺陷编号与下一步计划。>